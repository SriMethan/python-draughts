#!/usr/bin/env python
# -*- coding: utf-8 -*-

from __future__ import print_function, unicode_literals

import draughts
from draughts import CSA
import random
import sys
import time

if len(sys.argv) != 4:
    print('Usage: {0} host_name user_name password'.format(sys.argv[0]))
    sys.exit(1)

ct = CSA.TCPProtocol(sys.argv[1], 4081)
ct.login(sys.argv[2], sys.argv[3])
game_summary = ct.wait_match()
fen = game_summary['summary']['fen']
my_color = game_summary['my_color']
board = draughts.Board(fen)
ct.agree()

while True:
    print("pc turn")
    moves = []
    for move in board.legal_moves:
        moves.append(move)

    if len(moves) == 0:
        ct.resign()
        break

    next_move = random.choice(moves)
    board.push(next_move)
    print(board.pdn_str())
    print('pc turn end. and waiting Player')
    recieved_cmd = ct.move(
            board.pieces[next_move.to_square], my_color, next_move)

    print('Player moved')
    (_, hub, spend_time, message) = ct.parse_server_message(
            recieved_cmd, board)
    move = draughts.Move.from_hub(hub)
    board.push(move)
    print(board.pdn_str())
    print('Player moving handle end. and now pc turn')
